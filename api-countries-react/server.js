const express = require("express")
const cors = require("cors")
const { country, teams } = require("./dataCountries")

app.get("/country/", (req, res) => {

    
})

app.listen(port, () => {
    console.log("Server à l'écoute dans le port 8000");
})